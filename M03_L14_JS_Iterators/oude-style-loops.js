/* While loop:

Maak en nieuwe map en nieuwe javascript file.
Maak een array genaamd colors met de kleuren: yellow, blue, red, orange.
Schrijf een while loop die de kleuren in de console logged en stopt 
wanneer alle kleuren geprint zijn. */
// Ik geef een var die ik colors noem en met een array van de kleuren.
let colors = ['yellow', 'blue', 'red', 'orange'];
//Ik zet het begingetal op 0 en noem het n.
let n = 0;
//Ik wil de kleuren / objecten kleiner dan 4 genoemd hebben n+1, dus...
while (colors < 4) {
  n++;
}
console.log(colors);

//For loop:

/* Gebruik dezelfde array als startpunt.
Schrijf een for loop die de kleuren in de console logged en stopt wanneer alle kleuren geprint zijn.
Gebruik de lengte van de array in je for loop: array.length */

let colorsl = ['yellow', 'blue', 'red', 'orange'];
for (i = 1; i <= colorsl.length; i++) {
}
console.log(colorsl);